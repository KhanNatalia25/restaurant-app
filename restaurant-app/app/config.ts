// Базовый URL API бэкенда (Flask). Используется во всех сетевых запросах через apiClient.
// Единая константа позволяет избежать дублирования адреса сервера в каждом запросе
// и упрощает переключение между окружениями (dev/prod).
export const API_URL = "http://localhost:5000/api";
